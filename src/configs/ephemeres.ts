// NOTE: DO NOT DELETE THIS FILE
import { createConfig } from '~/utils';

export default createConfig({
    backend: 'http://localhost:3001',
    providerUrls: {
        upyun: 'https://repo-zorin.beaa.cn',
    },
});
